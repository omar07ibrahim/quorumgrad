# Repository provenance

This repository was created on 2021-08-10 under the name <code>RDP</code> and remained an empty Git repository: the GitHub API reported zero refs, zero branches, zero commits, and repository size 0 before it was repurposed.

On 2026-08-10 the same public repository (GitHub repository ID <code>394582438</code>) was renamed <code>quorumgrad</code> for a new, unrelated distributed machine-learning project.

There was no original source tree to delete, move, relicense, or claim as QuorumGrad work. A machine-readable inspection record will be preserved under <code>legacy/empty-rdp-2021/</code> in the next provenance commit.
